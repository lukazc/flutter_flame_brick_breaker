const gameWidth = 820.0;
const gameHeight = 1600.0;
const ballRadius = gameWidth * 0.02; // The design pattern of defining named constants as derived values enables you to modify the top level gameWidth and gameHeight to explore how the game look and feel changes as result.